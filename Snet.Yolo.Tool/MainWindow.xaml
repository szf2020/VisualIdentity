<snet:WindowBase
    x:Class="Snet.Yolo.Tool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Snet.Yolo.Tool"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:snet="https://shunnet.top"
    xmlns:ui="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:view="clr-namespace:Snet.Yolo.Tool.View"
    Title="{snet:Loc SystemTitle}"
    Width="1400"
    Height="1000"
    snet:ResxLocalizationProvider.DefaultAssembly="Snet.Yolo.Tool"
    snet:ResxLocalizationProvider.DefaultDictionary="Language"
    Icon="/Icon.ico"
    LoadAnimationEnabled="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox
            Grid.Row="0"
            Margin="5,5,5,0"
            Padding="0"
            ui:ElevationAssist.Elevation="Dp1"
            ui:GroupBoxAssist.HeaderPadding="7"
            Header="{Binding}"
            Style="{StaticResource MaterialDesignCardGroupBox}">
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <ui:PackIcon Grid.Column="0" Kind="Anchor" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="5,0,0,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{snet:Loc 菜单}"
                            TextAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </GroupBox.HeaderTemplate>
        </GroupBox>
        <TabControl
            Grid.Row="1"
            HorizontalContentAlignment="Center"
            ui:NavigationRailAssist.SelectionCornerRadius="{DynamicResource WindowCornerRadius}"
            ui:NavigationRailAssist.ShowSelectionBackground="True"
            Background="Transparent"
            BorderBrush="Transparent"
            BorderThickness="0"
            SnapsToDevicePixels="True"
            Style="{StaticResource MaterialDesignNavigationRailTabControl}"
            TabStripPlacement="Top">
            <TabItem Width="120" Margin="0,5,5,5">
                <TabItem.Header>
                    <StackPanel>
                        <ui:PackIcon
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Kind="ConsoleNetworkOutline" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Text="{snet:Loc YoloDetect}"
                            TextOptions.TextFormattingMode="Display" />
                    </StackPanel>
                </TabItem.Header>
                <view:YoloDetectView />
            </TabItem>
            <TabItem Width="120" Margin="0,5,5,5">
                <TabItem.Header>
                    <StackPanel>
                        <ui:PackIcon
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Kind="ConsoleNetworkOutline" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Text="{snet:Loc YoloPose}"
                            TextOptions.TextFormattingMode="Display" />
                    </StackPanel>
                </TabItem.Header>
                <view:YoloPoseView />
            </TabItem>
            <TabItem Width="120" Margin="0,5,5,5">
                <TabItem.Header>
                    <StackPanel>
                        <ui:PackIcon
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Kind="ConsoleNetworkOutline" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Text="{snet:Loc YoloSegment}"
                            TextOptions.TextFormattingMode="Display" />
                    </StackPanel>
                </TabItem.Header>
                <view:YoloSegmentView />
            </TabItem>
            <TabItem Width="120" Margin="0,5,5,5">
                <TabItem.Header>
                    <StackPanel>
                        <ui:PackIcon
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Kind="ConsoleNetworkOutline" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Text="{snet:Loc YoloOBB}"
                            TextOptions.TextFormattingMode="Display" />
                    </StackPanel>
                </TabItem.Header>
                <view:YoloOBBView />
            </TabItem>
            <TabItem Width="120" Margin="0,5,5,5">
                <TabItem.Header>
                    <StackPanel>
                        <ui:PackIcon
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Kind="ConsoleNetworkOutline" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                            Text="{snet:Loc YoloClassify}"
                            TextOptions.TextFormattingMode="Display" />
                    </StackPanel>
                </TabItem.Header>
                <view:YoloClassifyView />
            </TabItem>
        </TabControl>
    </Grid>
</snet:WindowBase>
